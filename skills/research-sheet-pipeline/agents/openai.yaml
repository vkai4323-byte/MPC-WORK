interface:
  display_name: "调研表格工作流"
  short_description: "快速全表筛选、批量调研与精确回填"
  default_prompt: "Use $research-sheet-pipeline's fixed fast path by default: scan the complete sheet before filtering, preserve exact keys, batch owner adapters, write once through the sheet owner, and verify the full live scope. Create checkpoints only for complex or resumable work."
