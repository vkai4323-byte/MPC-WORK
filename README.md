# MPC-WORK

Reusable workflow skills and supporting resources.

## Skills

- [`research-sheet-pipeline`](skills/research-sheet-pipeline/): API-first research, normalization, document, and spreadsheet writeback workflow with POPO and authenticated-browser fallbacks.

Install its Python dependencies with:

```bash
pip install -r skills/research-sheet-pipeline/requirements.txt
```
